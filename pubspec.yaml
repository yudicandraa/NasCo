name: watermelon_sound
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  audioplayers: ^5.2.0
  cupertino_icons: ^1.0.2
  dartz: ^0.10.1
  equatable: ^2.0.5
  file_picker: ^5.5.0
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  flutter_sound: ^9.2.13
  get_it: ^7.6.4
  http: ^0.13.6
  image: ^4.1.3
  image_picker: ^1.0.4
  just_audio: ^0.9.35
  microphone: ^0.1.0
  path_provider: ^2.0.2
  permission_handler: ^11.0.1
  rxdart: ^0.27.7
  tflite: ^1.1.2
  tflite_audio: ^0.3.0
dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
